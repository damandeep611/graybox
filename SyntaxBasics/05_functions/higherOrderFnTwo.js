// ? we will try to use HOF with some problems

// * Map()
// add 10 to each number in an array list
const addTen = [1, 2, 3, 4, 5, 6, 7];
const outputTen = addTen.map((num) => (num += 10));
console.log(outputTen);
