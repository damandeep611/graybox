// ? Assignment Operators
// use the += operator to add a number to a variable
let plusEqual = 22;

console.log((plusEqual += 8));

let bar = 10;
bar += 18;
console.log(bar);
// use the -= operator to subtract a number form a variable
let minusNow = 18;
minusNow -= 8;
console.log(minusNow);
