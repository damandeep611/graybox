// ? Ternary (conditonal) Operator
// program that uses ternary operator to check if a number is positive or negative
const numOne = 20;
const beverage = numOne >= 20 ? "Beer" : "Juice";
console.log(beverage);

// ? program that checks if a number is odd or even and log the result
const digit = 11;
const checkTi = digit % 2 == 0 ? "even" : "odd";
console.log(checkTi);