// ? program to print numbers from 1 to 10 using For loop

for (let step = 1; step <= 10; step++) {
  console.log(step);
}

// ? program to print table of 5 using for loop
for (let five = 5; five <= 50; five += 5) {
  console.log(five);
}
